{
  "rules": [
    {
      "allowed": {
        "origins": ["https://*.ftbhustle.com", "http://localhost:3000"],
        "methods": ["GET", "HEAD", "PUT"],
        "headers": ["Content-Type", "Cache-Control"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
